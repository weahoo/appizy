define(['src/appizy'], function (appizy) {
    "use strict";

    describe('Appizy setOutput', function () {
        it('should return ', function () {
            expect(appizy.setOutput('abc', 10, 'currency')).toBe(10);
        })
    });


});
